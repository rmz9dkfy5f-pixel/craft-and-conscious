# Decision Log

| Date | Decision | Reason | Alternatives Considered | Status |
|---|---|---|---|---|
| 2026-09-08 | Adopt Project Starter Kit v3.10.0, `web_application` profile, governance layer only (no optional modules) | Bring repo under AntBrainOS governance conventions without disrupting the working static site or forcing unneeded CI/security automation | Governance + CI/security modules enabled | Accepted |
| 2026-09-16 | Enable 10 Starter Kit optional modules; re-baseline 5 pre-existing governance docs to `project`-owned | Owner requested enabling all applicable modules; a pre-existing manifest/checksum drift on 5 files was found and fixed as part of the same work | Restore drifted files to template content instead | Accepted |
| 2026-09-17 | Set WCAG 2.2 AA as the accessibility conformance target; last-2-versions evergreen browsers (no IE11) as the support matrix; performance regressions tracked-not-blocked; all site imagery/code confirmed original/business-owned | Filling the 10 module policy docs required real policy answers rather than boilerplate; owner supplied each directly | WCAG 2.1 AA; broader legacy browser support; blocking releases on performance regressions | Accepted |
| 2026-09-17 (later) | Fix the 2 real color-contrast failures and the cart dialog's missing focus-trap/Escape/focus-on-open, found via a live Playwright audit against WCAG 2.2 AA, directly in `v9/index.html` | Owner-confirmed next task; verification with a real headless browser (not just CSS inspection) found genuine bugs, including correcting a wrong assumption in the prior session's own policy doc | Leave the (mistakenly assumed) `:focus-visible` gap as the only fix; ship only doc corrections without touching site code | Accepted |
| 2026-09-18 | Run an automated axe-core scan (not Lighthouse) against `v9/index.html`, scan-only first (report before fixing), then fix the 3 real Tier 1 findings it surfaced; tag this release `v0.1.1` | axe-core integrates directly with the existing Playwright setup and gives granular WCAG rule violations; owner chose report-first so findings could be reviewed before any further site-code change; this is the first tagged release since `v0.1.0` because it's the first actual site-content change (vs. governance-only work) since then | Lighthouse; fixing immediately without a review step; leaving this session's work untagged like prior governance-only sessions | Accepted |
